services:
  - type: web
    name: shift-calendar-app
    env: python
    plan: free
    buildCommand: ls -la && pwd && tree || echo "Tree command not available"
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
